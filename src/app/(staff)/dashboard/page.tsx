import { StubPage } from "@/components/layout/stub-page";

export default function DashboardPage() {
  return (
    <StubPage
      title="Operations"
      description="Supervisor dashboard ships later. This route keeps the shell navigable."
    />
  );
}
