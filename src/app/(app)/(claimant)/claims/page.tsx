import { StubPage } from "@/components/layout/stub-page";

export default function ClaimsPage() {
  return (
    <StubPage
      title="My claims"
      description="Claimant claim list ships in a later PBI. This route exists so the shell can navigate."
    />
  );
}
