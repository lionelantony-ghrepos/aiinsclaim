import { StubPage } from "@/components/layout/stub-page";

export default function ParametersPage() {
  return (
    <StubPage
      title="Parameters"
      description="Parameter CRUD ships with the admin rules PBI."
    />
  );
}
