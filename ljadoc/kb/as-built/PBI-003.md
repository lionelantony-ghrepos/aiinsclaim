# PBI-003 — Auth, roles and route protection

## PBI / ACs / TCs

- PBI: [PBI-003](../PRD.md#pbi-003)
- Phase: 1
- ACs: AC-003-01, AC-003-02, AC-003-03, AC-003-04 (see [TEST-PLAN](../TEST-PLAN.md))
- TCs: TC-003-01, TC-003-02, TC-003-03, TC-003-04
- GitHub: [PBI issue #3](https://github.com/lionelantony-ghrepos/aiinsclaim/issues/3)

## Shipped vs spec

| Item | Status | Notes |
| --- | --- | --- |
| iron-session + bcrypt login | partial | Session helpers stubbed in PBI-001; UI not built |
| Middleware route groups | not started |  |
| Six demo role accounts on login page | not started | Seed has 3 accounts only |

## Surfaces

`src/lib/auth/` (session exists as stub); `middleware.ts`; login/logout pages; nav filtered by DB role.

## Contracts

Zod auth form schemas in `src/lib/schemas/` (to add).

## Rules

None — no decision tables evaluated in this PBI.

## How to extend

Use existing `users` table + bcrypt; extend `requireRole()` in server actions.

## Ops

Env: `SESSION_SECRET`. Demo accounts via `npm run seed`.

## Trace

Pending implementation — land with commit `feat(PBI-003): …` and record SHA here.
