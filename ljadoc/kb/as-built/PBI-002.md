# PBI-002 — Design tokens + app layout shell

## PBI / ACs / TCs

- PBI: [PBI-002](../PRD.md#pbi-002)
- Phase: 0
- ACs: AC-002-01, AC-002-02, AC-002-03 (see [TEST-PLAN](../TEST-PLAN.md))
- TCs: TC-002-01, TC-002-02, TC-002-03
- GitHub: [PBI issue #2](https://github.com/lionelantony-ghrepos/aiinsclaim/issues/2) · [TC #24–26](https://github.com/lionelantony-ghrepos/aiinsclaim/issues?q=TC-002)

## Shipped vs spec

| Item | Status | Notes |
| --- | --- | --- |
| Ledger design tokens light+dark | done | CSS vars in `src/app/globals.css`; hex only in that file |
| App shell + role-aware sidebar | done | Shared shell; placeholder role switcher until PBI-003 |
| Six signature component stubs | done | Typed mock props on `/dev/components` |
| shadcn/ui init | partial | `components.json` present; Button/Badge/Card hand-rolled on Ledger tokens after CLI install failed on peer deps |

## Surfaces

- Tokens: `src/app/globals.css` (`:root` + `.dark`)
- Shell: `src/components/layout/app-shell.tsx` (sidebar, topbar, theme toggle, role preview)
- Nav: `src/lib/nav.ts` + `navItemsForRole()`
- Route groups: `(claimant)`, `(staff)`, `(admin)` stub pages; `/dev/components` lab
- Signature components: `ClaimStatusTimeline`, `TaskCard`, `AgentProposalCard`, `FraudBandBadge`, `SlaCountdown`, `DecisionTableGrid`

## Contracts

Component prop types only; no API payloads. Theme stored in `aiinsclaim-theme`; preview role in `aiinsclaim.preview-role`.

## Rules

None — no decision tables evaluated in this PBI (UI-only). SLA amber ratio is DESIGN §9 visual (`SLA_AMBER_ELAPSED_RATIO` in `src/lib/ui/sla-visual.ts`).

## How to extend

- PBI-003: replace the preview role switcher with `getCurrentUser()` / `requireRole()` and filter `navItemsForRole` from the session.
- Add shadcn primitives via CLI into `src/components/ui/` — keep consuming Ledger CSS vars, no raw hex in components.
- New nav destinations go in `src/lib/nav.ts` with an explicit role list.

## Ops

No new env vars. Playwright + axe: `tests/e2e/pbi-002.spec.ts`. Unit: `tests/lib/nav.test.ts`, `tests/ui/tokens.test.ts`, `tests/ui/sla-visual.test.ts`.

## Trace

- Commit: `feat(PBI-002): add Ledger design tokens and app layout shell`
- SHA: (filled after commit)
