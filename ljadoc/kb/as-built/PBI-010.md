# PBI-010 — Document upload and extraction (AGT-EXTRACT)

## PBI / ACs / TCs

- PBI: [PBI-010](../PRD.md#pbi-010)
- Phase: 2
- ACs: AC-010-01, AC-010-02, AC-010-03, AC-010-04, AC-010-05 (see [TEST-PLAN](../TEST-PLAN.md))
- TCs: TC-010-01, TC-010-02, TC-010-03, TC-010-04, TC-010-05, TC-010-06
- GitHub: [PBI issue #10](https://github.com/lionelantony-ghrepos/aiinsclaim/issues/10) · TC [#62–67](https://github.com/lionelantony-ghrepos/aiinsclaim/issues/62)

## Shipped vs spec

| Item | Status | Notes |
| --- | --- | --- |
| Upload validation (mime/size from parameters) | done | `doc.allowed_mimes`, `doc.max_bytes` |
| Local storage + documents row | done | Extends PBI-001/009 upload |
| AGT-EXTRACT agent + gateway mock | done | Retry once on schema fail |
| Extraction pipeline (auto-apply vs HITL) | done | Threshold via `doc.autoaccept_confidence` |
| Document preview route | done | `GET /api/documents/:id/preview` |
| Staff verify-extraction UI | done | `/verify-extraction/[taskId]` |
| FNOL document status chips | done | Wizard documents step |
| Re-extract action | deferred | Out of explicit AC scope for this slice |
| InsForge signed URLs / live vision | deferred | Local preview + mock gateway in CI |

## Surfaces

- Routes: `/api/documents/[id]/preview`, `/verify-extraction/[taskId]`
- Packages: `src/lib/agents/extract.ts`, `src/lib/extraction/pipeline.ts`, `src/lib/extraction/apply.ts`, `src/lib/documents/validate-upload.ts`
- UI: `src/components/documents/document-status-badge.tsx`, `src/components/documents/verify-extraction-form.tsx`, FNOL wizard documents step
- DB: `documents`, `extractions`, `tasks`, `agent_runs` (no schema change)
- Agents: AGT-EXTRACT v1 mock + optional live gateway

## Contracts

- Zod: `src/lib/schemas/agents/extract.ts` (per-doc-type extraction + `VerifyExtractionSchema`)
- Server actions: `uploadDocumentAction` (claimant + staff), `verifyExtractionAction`
- API: `GET /api/documents/:id/preview`

## Rules

- Auto-apply when `minConfidence ≥ doc.autoaccept_confidence` (parameter)
- STP guard reads extraction confidence via `stp.min_extraction_confidence` (existing)
- BR-DOC-001 checklist unchanged; extraction status shown separately
- Staff roles: intake_agent, adjuster, supervisor, admin for verify flow

## How to extend

- PBI-011: call `retriageClaim` after `applyExtractionFields`
- Add re-extract server action on document row
- Wire live AI Gateway vision when `AI_API_KEY` + `AI_BASE_URL` set
- PBI-013: link verify tasks into staff queue worklist

## Ops

- Env: `STORAGE_PATH`, optional `AI_API_KEY`, `AI_BASE_URL`
- Seed: `doc.allowed_mimes` parameter added
- Tests: `tests/documents/pbi-010-upload.test.ts`, `tests/agents/pbi-010-extract.test.ts`, `tests/e2e/pbi-010.spec.ts`

## Trace

- Commit: `feat(pbi-010): document upload and AGT-EXTRACT pipeline`
- SHA: (fill at commit time)
