# PBI-016 — Settlement, authority and payments

## PBI / ACs / TCs

- PBI: [PBI-016](../PRD.md#pbi-016)
- Phase: 2
- ACs: AC-016-01, AC-016-02, AC-016-03, AC-016-04, AC-016-05 (see [TEST-PLAN](../TEST-PLAN.md))
- TCs: TC-016-01, TC-016-02, TC-016-03, TC-016-04, TC-016-05
- GitHub: [PBI issue #16](https://github.com/lionelantony-ghrepos/aiinsclaim/issues/16) · [TC #95–99](https://github.com/lionelantony-ghrepos/aiinsclaim/issues/95)

## Shipped vs spec

| Item | Status | Notes |
| --- | --- | --- |
| Propose settlement (items − deductible) | done | Writes `settlements` row (`proposed`); math in `settlement-math.ts` |
| BR-AUTH-001 approve / route / SIU block | done | `approveSettlement` evaluates BR-AUTH-001; `require_next_level` → supervision `approve_settlement` task + `pending_approval`; `block` → `SIU_HOLD` banner |
| Guard requires `authInputs` | done | `in_settlement → approved` guard refuses without `authInputs` (no empty-object fallback) |
| Mock payment (ach_mock / check_mock) | done | Issues `payments` row + reference; transitions claim → `paid` |
| Close checklist | done | `paid → closed` via `closure_checklist` (zero open tasks) |
| Denial with reason + supervisor confirm | done | Codes from `denial.reason_codes` parameter; escalation task kind `deny_confirmation`; supervisor accept → `denied` |
| Real payment rails | deferred | Out of scope — mock methods only |

## Surfaces

- Routes / panels: `(staff)/claims/[id]` Financials tab — `settlement-workbench.tsx` (propose/approve/pay/close/deny); SIU-hold and authority-routed banners; supervisor resolves via queue task detail (`approve_settlement` / `deny_confirmation`)
- Packages / modules: `src/lib/settlement/service.ts`, `src/lib/settlement-math.ts`, `src/lib/schemas/financials.ts`, `src/lib/schemas/denial.ts`; guards in `src/lib/state-machine/guards.ts`
- DB / migrations: `settlements` table (items_json, deductible_applied, total_amount, status, authority_rule_audit_id); uses existing `payments`, `tasks`, `notifications`, claim `denial_reason_code`
- Agents: none (rules-only authority; no settlement agent)

## Contracts

- Zod schemas (`src/lib/schemas/`): `ProposeSettlementSchema`, `ApproveSettlementSchema`, `IssuePaymentSchema`, `CloseClaimSchema`, `DenyClaimSchema`; `DenialReasonEnum` / `DENIAL_REASON_CODES` in `denial.ts` (must match seeded `denial.reason_codes`)
- Server actions (`claims/[id]/actions.ts`): `proposeSettlementAction`, `approveSettlementAction` (may return `SIU_HOLD` / routed), `issuePaymentAction`, `closeClaimAction`, `denyClaimAction`; task resolve hooks for approve/deny confirmation
- Generated reference: [generated/](../generated/) (after `npm run docs:generate`)

## Rules

- Decision tables evaluated or seeded: BR-AUTH-001 (IDs only); parameter key `denial.reason_codes` (codes only — no thresholds here)
- Entitlements / `requireRole()`: settlement actions require adjuster/supervisor/admin (`ASSESSMENT_ROLES`); writes scoped via `canWriteClaim`

## How to extend

- Authority remains server-side at approve time via BR-AUTH-001 + required `authInputs` on the state-machine guard — do not approve from the client
- New payment method: extend `PAYMENT_METHODS` enum + `IssuePaymentSchema`; keep mocks until a real rail PBI
- Denial codes: update seeded `denial.reason_codes` and `DENIAL_REASON_CODES` together

## Ops

- Env vars (names only, never values): none new
- Seed / fixtures: `in_settlement` seed claims for e2e; unit helpers in `tests/helpers/pbi-016-fixtures.ts`; rules/parameters via seed loaders
- Test harness notes: `tests/settlement/pbi-016-settlement.test.ts`, `tests/e2e/pbi-016.spec.ts`; guard coverage also in `tests/state-machine/pbi-007-transitions.test.ts`

## Trace

- Commit: `feat(pbi-016): settlement authority, mock payment, and denial`
- SHA: `037840fda52dec16af45b504d1bfdf38b33193ab`
- Follow-up: `fix(pbi-016): require real authority inputs and block duplicate routes` — `613dc0ac53eabeae99d694b1f17825c02d16dc6c`
