## PBI-003: Auth, roles and route protection

| Field | Value |
|-------|-------|
| **Phase** | 1 |
| **MoSCoW** | must |
| **Depends on** | PBI-001, PBI-002 |

### Main prompt (every session)

You are building **aiinsclaim**, an AI-native agentic insurance claims system (Auto + Property, learning stack on mock data). Obey `.cursor/rules/` and `ljadoc/AGENTS.md`. Read `ljadoc/DESIGN.md` and `ljadoc/BUSINESS-RULES.md` before coding. Read `ljadoc/kb/INDEX.md` and dependency as-built files for the current PBI.

TypeScript strict; Zod-validate all boundaries (`src/lib/schemas/`). No business logic hard-coded in components — logic lives in BR-* decision tables (`parameters` table) or packages under `src/lib/rules/`. SQLite + Drizzle for data; scope every query via `src/lib/auth/scope.ts`.

Write Vitest unit tests and Playwright e2e per `ljadoc/TEST-PLAN.md` TC IDs for the PBI. Update the traceability status in `ljadoc/kb/as-built/PBI-003.md` (and `ljadoc/kb/INDEX.md` if status changed) when tests pass — same commit as code. Run `npm run docs:kb-check` before done.

### Knowledge base (this PBI)

**Before coding:**
1. Read `ljadoc/kb/INDEX.md`, `ljadoc/kb/traceability.md`, and dependency as-built: `ljadoc/kb/as-built/PBI-001.md`, `ljadoc/kb/as-built/PBI-002.md`
2. Confirm AC/TC IDs for PBI-003 in TEST-PLAN

**After shipping:**
1. Copy/update `ljadoc/kb/as-built/PBI-003.md` from `ljadoc/kb/_template-as-built.md` (BR-* IDs only; no thresholds)
2. Update `ljadoc/kb/INDEX.md` status column if changed
3. Add `ljadoc/kb/adr/` only if a durable choice is missing from DESIGN.md
4. Patch `ljadoc/08-User-Guide.md` only for user-visible UI
5. Run `npm run docs:generate` if schemas/packages changed; `npm run docs:kb-check`
6. Do not index `ljadoc/` into future claims copilot RAG unless explicitly scheduled

### User story

As a user, I sign in and see only what my role permits.

### Scope

iron-session + bcrypt auth, middleware route groups, login/logout, demo accounts per role.

### Definition of done

Six demo roles sign in correctly; AC-003-01..04.

### Cursor prompt

> **Context:** DESIGN §2.1, §10; DATA-DICTIONARY §users; builds on PBI-001/002.
> **Task:** Implement local session auth (iron-session + bcrypt). Ensure `users` table in Drizzle schema per DATA-DICTIONARY. Server helpers `getCurrentUser()`/`requireRole(...)` in `src/lib/auth/`; Next middleware guarding `(claimant)`, `(staff)`, `(admin)` groups; login/logout pages; sidebar nav filtered by real role.
> **Constraints:** roles from DB only; no client-side-only gating; Zod on auth forms.
> **Files expected:** `src/lib/auth/`, `middleware.ts`, login page, nav update.
> **Acceptance check:** AC-003-01..04 (role redirects, deep-link protection).
> **Out of scope:** all other tables; password reset flows.

### References

- [00-MAIN-PROMPT](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/00-MAIN-PROMPT.md)
- [PRD](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/PRD.md#pbi-003)
- [TEST-PLAN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/TEST-PLAN.md)
- [DESIGN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/DESIGN.md)
- [KB INDEX](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/INDEX.md)
- [As-built PBI-003](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/as-built/PBI-003.md)