## PBI-018: Ops dashboards and audit viewer

| Field | Value |
|-------|-------|
| **Phase** | 2 |
| **MoSCoW** | should |
| **Depends on** | PBI-014, PBI-016 |

### Main prompt (every session)

You are building **aiinsclaim**, an AI-native agentic insurance claims system (Auto + Property, learning stack on mock data). Obey `.cursor/rules/` and `ljadoc/AGENTS.md`. Read `ljadoc/DESIGN.md` and `ljadoc/BUSINESS-RULES.md` before coding. Read `ljadoc/kb/INDEX.md` and dependency as-built files for the current PBI.

TypeScript strict; Zod-validate all boundaries (`src/lib/schemas/`). No business logic hard-coded in components — logic lives in BR-* decision tables (`parameters` table) or packages under `src/lib/rules/`. SQLite + Drizzle for data; scope every query via `src/lib/auth/scope.ts`.

Write Vitest unit tests and Playwright e2e per `ljadoc/TEST-PLAN.md` TC IDs for the PBI. Update the traceability status in `ljadoc/kb/as-built/PBI-018.md` (and `ljadoc/kb/INDEX.md` if status changed) when tests pass — same commit as code. Run `npm run docs:kb-check` before done.

### Knowledge base (this PBI)

**Before coding:**
1. Read `ljadoc/kb/INDEX.md`, `ljadoc/kb/traceability.md`, and dependency as-built: `ljadoc/kb/as-built/PBI-014.md`, `ljadoc/kb/as-built/PBI-016.md`
2. Confirm AC/TC IDs for PBI-018 in TEST-PLAN

**After shipping:**
1. Copy/update `ljadoc/kb/as-built/PBI-018.md` from `ljadoc/kb/_template-as-built.md` (BR-* IDs only; no thresholds)
2. Update `ljadoc/kb/INDEX.md` status column if changed
3. Add `ljadoc/kb/adr/` only if a durable choice is missing from DESIGN.md
4. Patch `ljadoc/08-User-Guide.md` only for user-visible UI
5. Run `npm run docs:generate` if schemas/packages changed; `npm run docs:kb-check`
6. Do not index `ljadoc/` into future claims copilot RAG unless explicitly scheduled

### User story

As supervisor/admin, I see operational health KPIs and inspect any decision chain.

### Scope

Dashboard KPIs/charts, audit viewer with CSV export.

### Definition of done

Dashboard matches seed; STP audit chain inspectable; AC-018-01..04.

### Cursor prompt

> **Context:** DESIGN §7 (override telemetry), §9; depends on PBI-014/016.
> **Task:** Create SQL views (migration) for KPIs (cycle time, stp_rate, breach counts, override rates per agent_id); dashboard page `(staff)/dashboard` (supervisor+) with KPI cards + recharts-style charts on design tokens, backlog table; audit viewer `(staff)/claims/[id]/audit` rendering full decision chain (rule_audit_log rows expandable to matched rules & version, agent_runs expandable to redacted I/O, state history) + CSV export server action.
> **Constraints:** views not client-side aggregation; audit read-only; charts AA (patterns + labels, not color-only).
> **Files expected:** views migration, dashboard + audit routes/components, export action.
> **InsForge actions:** views migration.
> **Acceptance check:** AC-018-01..04.
> **Out of scope:** NL querying (PBI-019), scheduled reports.

### References

- [00-MAIN-PROMPT](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/00-MAIN-PROMPT.md)
- [PRD](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/PRD.md#pbi-018)
- [TEST-PLAN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/TEST-PLAN.md)
- [DESIGN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/DESIGN.md)
- [KB INDEX](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/INDEX.md)
- [As-built PBI-018](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/as-built/PBI-018.md)