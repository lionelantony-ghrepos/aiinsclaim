## PBI-005: Seed / mock-data pipeline

| Field | Value |
|-------|-------|
| **Phase** | 1 |
| **MoSCoW** | must |
| **Depends on** | PBI-004, PBI-006 |

### Main prompt (every session)

You are building **aiinsclaim**, an AI-native agentic insurance claims system (Auto + Property, learning stack on mock data). Obey `.cursor/rules/` and `ljadoc/AGENTS.md`. Read `ljadoc/DESIGN.md` and `ljadoc/BUSINESS-RULES.md` before coding. Read `ljadoc/kb/INDEX.md` and dependency as-built files for the current PBI.

TypeScript strict; Zod-validate all boundaries (`src/lib/schemas/`). No business logic hard-coded in components — logic lives in BR-* decision tables (`parameters` table) or packages under `src/lib/rules/`. SQLite + Drizzle for data; scope every query via `src/lib/auth/scope.ts`.

Write Vitest unit tests and Playwright e2e per `ljadoc/TEST-PLAN.md` TC IDs for the PBI. Update the traceability status in `ljadoc/kb/as-built/PBI-005.md` (and `ljadoc/kb/INDEX.md` if status changed) when tests pass — same commit as code. Run `npm run docs:kb-check` before done.

### Knowledge base (this PBI)

**Before coding:**
1. Read `ljadoc/kb/INDEX.md`, `ljadoc/kb/traceability.md`, and dependency as-built: `ljadoc/kb/as-built/PBI-004.md`, `ljadoc/kb/as-built/PBI-006.md`
2. Confirm AC/TC IDs for PBI-005 in TEST-PLAN

**After shipping:**
1. Copy/update `ljadoc/kb/as-built/PBI-005.md` from `ljadoc/kb/_template-as-built.md` (BR-* IDs only; no thresholds)
2. Update `ljadoc/kb/INDEX.md` status column if changed
3. Add `ljadoc/kb/adr/` only if a durable choice is missing from DESIGN.md
4. Patch `ljadoc/08-User-Guide.md` only for user-visible UI
5. Run `npm run docs:generate` if schemas/packages changed; `npm run docs:kb-check`
6. Do not index `ljadoc/` into future claims copilot RAG unless explicitly scheduled

### User story

As a developer/demo presenter, I run one command and every screen has realistic data.

### Scope

Deterministic seed (faker seed 42), 120 claims distribution, rules seeded via engine, local doc assets.

### Definition of done

npm run seed under 2 min; AC-005-01..04.

### Cursor prompt

> **Context:** DATA-DICTIONARY §6; BUSINESS-RULES.md (all tables + defaults); depends on PBI-004 (and PBI-006 evaluator — build seed to import it).
> **Task:** Build `/seed` pipeline: generators per entity honoring §6.1–6.3 distributions; loader that seeds rule sets/versions/rules/conditions/actions for all 9 BR-* tables and ~30 parameters with BUSINESS-RULES defaults; claims seeded by invoking `evaluateRuleSet` so route/fraud/reserve values carry genuine `rule_audit_log` rows; upload `/seed/assets` files to Storage. npm scripts `seed`, `seed:rules`, `seed:demo` with production guard.
> **Constraints:** idempotent; deterministic; no PII-looking real data (faker).
> **Files expected:** `/seed/**`, `package.json` scripts, `/seed/assets/*` (generate simple PDFs/JPEGs).
> **InsForge actions:** storage bucket `claim-documents` (private); bulk inserts.
> **Acceptance check:** AC-005-01..04 (counts, distribution spot checks, rerun idempotency).
> **Out of scope:** UI.

### References

- [00-MAIN-PROMPT](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/00-MAIN-PROMPT.md)
- [PRD](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/PRD.md#pbi-005)
- [TEST-PLAN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/TEST-PLAN.md)
- [DESIGN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/DESIGN.md)
- [KB INDEX](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/INDEX.md)
- [As-built PBI-005](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/as-built/PBI-005.md)