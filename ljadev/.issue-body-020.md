## PBI-020: Comms drafting agent (AGT-COMMS)

| Field | Value |
|-------|-------|
| **Phase** | 3 |
| **MoSCoW** | could |
| **Depends on** | PBI-017 |

### Main prompt (every session)

You are building **aiinsclaim**, an AI-native agentic insurance claims system (Auto + Property, learning stack on mock data). Obey `.cursor/rules/` and `ljadoc/AGENTS.md`. Read `ljadoc/DESIGN.md` and `ljadoc/BUSINESS-RULES.md` before coding. Read `ljadoc/kb/INDEX.md` and dependency as-built files for the current PBI.

TypeScript strict; Zod-validate all boundaries (`src/lib/schemas/`). No business logic hard-coded in components — logic lives in BR-* decision tables (`parameters` table) or packages under `src/lib/rules/`. SQLite + Drizzle for data; scope every query via `src/lib/auth/scope.ts`.

Write Vitest unit tests and Playwright e2e per `ljadoc/TEST-PLAN.md` TC IDs for the PBI. Update the traceability status in `ljadoc/kb/as-built/PBI-020.md` (and `ljadoc/kb/INDEX.md` if status changed) when tests pass — same commit as code. Run `npm run docs:kb-check` before done.

### Knowledge base (this PBI)

**Before coding:**
1. Read `ljadoc/kb/INDEX.md`, `ljadoc/kb/traceability.md`, and dependency as-built: `ljadoc/kb/as-built/PBI-017.md`
2. Confirm AC/TC IDs for PBI-020 in TEST-PLAN

**After shipping:**
1. Copy/update `ljadoc/kb/as-built/PBI-020.md` from `ljadoc/kb/_template-as-built.md` (BR-* IDs only; no thresholds)
2. Update `ljadoc/kb/INDEX.md` status column if changed
3. Add `ljadoc/kb/adr/` only if a durable choice is missing from DESIGN.md
4. Patch `ljadoc/08-User-Guide.md` only for user-visible UI
5. Run `npm run docs:generate` if schemas/packages changed; `npm run docs:kb-check`
6. Do not index `ljadoc/` into future claims copilot RAG unless explicitly scheduled

### User story

Draft claimant communications from templates + summary; human edits and sends.

### Scope

Ack/info/decision letter drafts, mock outbox, never auto-send.

### Definition of done

AC-020-01..03.

### Cursor prompt

> **Context:** AGENT-OPS §AGT-COMMS; depends on PBI-017.
> **Task:** Implement comms drafting agent: ack/info-request/decision letter drafts from templates + claim summary; editable preview; mock outbox (never auto-send); tone/reading-level validated on output schema; drafts logged to agent_runs with prompt version.
> **Constraints:** human must explicitly send; no autonomous outbound comms; no PII in logs beyond claim IDs.
> **Files expected:** `src/lib/agents/comms.ts`, draft UI in workbench, mock outbox, server actions.
> **Acceptance check:** AC-020-01..03.
> **Out of scope:** real email/SMS integrations.

### References

- [00-MAIN-PROMPT](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/00-MAIN-PROMPT.md)
- [PRD](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/PRD.md#pbi-020)
- [TEST-PLAN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/TEST-PLAN.md)
- [DESIGN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/DESIGN.md)
- [KB INDEX](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/INDEX.md)
- [As-built PBI-020](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/as-built/PBI-020.md)