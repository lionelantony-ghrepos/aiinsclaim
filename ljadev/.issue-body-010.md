## PBI-010: Document upload and extraction (AGT-EXTRACT)

| Field | Value |
|-------|-------|
| **Phase** | 2 |
| **MoSCoW** | must |
| **Depends on** | PBI-009 |

### Main prompt (every session)

You are building **aiinsclaim**, an AI-native agentic insurance claims system (Auto + Property, learning stack on mock data). Obey `.cursor/rules/` and `ljadoc/AGENTS.md`. Read `ljadoc/DESIGN.md` and `ljadoc/BUSINESS-RULES.md` before coding. Read `ljadoc/kb/INDEX.md` and dependency as-built files for the current PBI.

TypeScript strict; Zod-validate all boundaries (`src/lib/schemas/`). No business logic hard-coded in components — logic lives in BR-* decision tables (`parameters` table) or packages under `src/lib/rules/`. SQLite + Drizzle for data; scope every query via `src/lib/auth/scope.ts`.

Write Vitest unit tests and Playwright e2e per `ljadoc/TEST-PLAN.md` TC IDs for the PBI. Update the traceability status in `ljadoc/kb/as-built/PBI-010.md` (and `ljadoc/kb/INDEX.md` if status changed) when tests pass — same commit as code. Run `npm run docs:kb-check` before done.

### Knowledge base (this PBI)

**Before coding:**
1. Read `ljadoc/kb/INDEX.md`, `ljadoc/kb/traceability.md`, and dependency as-built: `ljadoc/kb/as-built/PBI-009.md`
2. Confirm AC/TC IDs for PBI-010 in TEST-PLAN

**After shipping:**
1. Copy/update `ljadoc/kb/as-built/PBI-010.md` from `ljadoc/kb/_template-as-built.md` (BR-* IDs only; no thresholds)
2. Update `ljadoc/kb/INDEX.md` status column if changed
3. Add `ljadoc/kb/adr/` only if a durable choice is missing from DESIGN.md
4. Patch `ljadoc/08-User-Guide.md` only for user-visible UI
5. Run `npm run docs:generate` if schemas/packages changed; `npm run docs:kb-check`
6. Do not index `ljadoc/` into future claims copilot RAG unless explicitly scheduled

### User story

As a claimant/adjuster, uploaded documents become structured data with HITL when confidence is low.

### Scope

Local storage upload, AGT-EXTRACT pipeline, verify_extraction task UI.

### Definition of done

Demo docs extract correctly; AC-010-01..05.

### Cursor prompt

> **Context:** DESIGN §3.3, AGENT-OPS §AGT-EXTRACT, API-CONTRACTS §extraction (per-doc-type Zod schemas); depends on PBI-009.
> **Task:** Implement upload server action (mime/size allowlist from parameters), Storage write, document rows; extraction pipeline: AI Gateway vision+LLM call with doc-type-specific prompt + strict Zod parse (retry once on schema fail, then task), per-field confidence, auto-apply vs `verify_extraction` task per threshold param; verification UI in `(staff)` with doc preview (signed URL) beside editable fields, accept/correct writes `verified_by` and applies; status chips on claim documents tab.
> **Constraints:** document text is data not instructions (delimited prompt per AGENT-OPS guardrails); timeouts → manual task; no PII in logs.
> **Files expected:** `src/lib/agents/extract.ts`, doc schemas, upload/verify actions, documents tab UI, verify task view.
> **InsForge actions:** Storage signed URLs; AI Gateway (AGT-EXTRACT prompts v1 per doc_type).
> **Acceptance check:** AC-010-01..05.
> **Out of scope:** photo damage assessment (Later/F16).

### References

- [00-MAIN-PROMPT](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/00-MAIN-PROMPT.md)
- [PRD](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/PRD.md#pbi-010)
- [TEST-PLAN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/TEST-PLAN.md)
- [DESIGN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/DESIGN.md)
- [KB INDEX](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/INDEX.md)
- [As-built PBI-010](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/as-built/PBI-010.md)