## PBI-016: Settlement, authority and payments

| Field | Value |
|-------|-------|
| **Phase** | 2 |
| **MoSCoW** | must |
| **Depends on** | PBI-015 |

### Main prompt (every session)

You are building **aiinsclaim**, an AI-native agentic insurance claims system (Auto + Property, learning stack on mock data). Obey `.cursor/rules/` and `ljadoc/AGENTS.md`. Read `ljadoc/DESIGN.md` and `ljadoc/BUSINESS-RULES.md` before coding. Read `ljadoc/kb/INDEX.md` and dependency as-built files for the current PBI.

TypeScript strict; Zod-validate all boundaries (`src/lib/schemas/`). No business logic hard-coded in components — logic lives in BR-* decision tables (`parameters` table) or packages under `src/lib/rules/`. SQLite + Drizzle for data; scope every query via `src/lib/auth/scope.ts`.

Write Vitest unit tests and Playwright e2e per `ljadoc/TEST-PLAN.md` TC IDs for the PBI. Update the traceability status in `ljadoc/kb/as-built/PBI-016.md` (and `ljadoc/kb/INDEX.md` if status changed) when tests pass — same commit as code. Run `npm run docs:kb-check` before done.

### Knowledge base (this PBI)

**Before coding:**
1. Read `ljadoc/kb/INDEX.md`, `ljadoc/kb/traceability.md`, and dependency as-built: `ljadoc/kb/as-built/PBI-015.md`
2. Confirm AC/TC IDs for PBI-016 in TEST-PLAN

**After shipping:**
1. Copy/update `ljadoc/kb/as-built/PBI-016.md` from `ljadoc/kb/_template-as-built.md` (BR-* IDs only; no thresholds)
2. Update `ljadoc/kb/INDEX.md` status column if changed
3. Add `ljadoc/kb/adr/` only if a durable choice is missing from DESIGN.md
4. Patch `ljadoc/08-User-Guide.md` only for user-visible UI
5. Run `npm run docs:generate` if schemas/packages changed; `npm run docs:kb-check`
6. Do not index `ljadoc/` into future claims copilot RAG unless explicitly scheduled

### User story

As an adjuster I propose settlements; authority matrix decides approval; mock payments issue.

### Scope

Settlement panel, BR-AUTH-001, mock payment, closure checklist, denial path.

### Definition of done

Claim runs settlement to paid to closed; AC-016-01..05.

### Cursor prompt

> **Context:** DESIGN §4.2; BUSINESS-RULES BR-AUTH-001 (incl. worked example); depends on PBI-015.
> **Task:** Implement settlement panel in workbench Financials tab: proposal builder (items − deductible, Zod-validated), approve action evaluating BR-AUTH-001 (allow → transition; require_next_level → supervision task with proposal payload; block → SIU-hold banner), supervisor approval task view (approve/reject with reason), mock payment issuance action + payment record, closure checklist + close action, denial dialog (reason codes from parameters list, supervisor confirmation task). Notifications throughout.
> **Constraints:** authority checked server-side at approval time (not just UI); every financial action → audit_log; no real payment integrations.
> **Files expected:** settlement/payment components + server actions, supervisor approval view.
> **InsForge actions:** none new.
> **Acceptance check:** AC-016-01..05 (worked example 32k→supervision; SIU block; pay/close; denial).
> **Out of scope:** recoveries/subrogation (Later/F20).

### References

- [00-MAIN-PROMPT](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/00-MAIN-PROMPT.md)
- [PRD](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/PRD.md#pbi-016)
- [TEST-PLAN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/TEST-PLAN.md)
- [DESIGN](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/DESIGN.md)
- [KB INDEX](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/INDEX.md)
- [As-built PBI-016](https://github.com/lionelantony-ghrepos/aiinsclaim/blob/feature/ljadoc/kb/as-built/PBI-016.md)